export default function PropsChildren({ children }) {
     // console.log(props)
     return (

          <div>
               {children}
               {/* {props.children} */}

          </div>
     )
}